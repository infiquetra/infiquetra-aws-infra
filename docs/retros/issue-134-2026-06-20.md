# Retro — C0.3 aws-infra service registry + per-service OIDC deploy roles · 2026-06-20

**Scope.** thread-scoped — saga `issue-134`, issues infiquetra-aws-infra#134 (C0.3-S3) + #135 (C0.3-S4), PR #137, parent campps-platform#10.
**Evidence freshness.** verified at HEAD — PR #137 `MERGED` at `0a69c19`; both issues `CLOSED` 2026-06-20; U4 nonprod roles re-verified live in IAM (account `477152411873`). Thread-scoped, so the Pass-0 window guard does not apply.

## What shipped
- **U1 — 6 backend services registered** (`coppa-consent`, `registration`, `payments`, `health-forms`, `activities-achievements`, `staff-management`), each `serverless-api`, all 3 envs — registry-driven minting auto-creates each role + 3 policies + boundary. PR #137 / `0a69c19`.
- **U2 — `web-app` least-privilege deploy profile** + `campps-web-app` registered (S3 static-site + CloudFront only, zero backend grants; provisional per KTD3).
- **U3 — unknown-profile guard** — `_create_deploy_policies()` now raises `ValueError` instead of silently defaulting to `serverless-api`.
- **U4 — nonprod deploy** — `CamppsNonProdDeployRolesStack` → `477152411873`, additive-only (40 adds / 0 modify-delete), `UPDATE_COMPLETE`. **R5: 7/7 roles** exist with env-scoped trust.
- Tests: 19 new; ruff / mypy --strict / cdk synth / pytest green; 5-lens adversarial review PASS (0 P0/P1/P2).

## Findings (the structured core)
- **Doc-review caught 2 P1s before any code was written** — the R5/U4 deploy mechanism and the assume-verification gap. The single highest-leverage moment of the thread: the "merge ≠ deploy" reality was surfaced at the plan stage, not after a green merge. Evidence: saga ticks `20260620-013212` / `-014734`. Promotion: none (process win, captured here).
- **Merge gives zero roles in AWS — the bootstrap app is outside CD.** `deploy-infrastructure.yml` deploys only the management `app.py` (org/SSO); the registry-driven roles live in `app_campps_bootstrap.py`, which no workflow runs. Confirmed at tick `20260620-035055`. **Recurrence** of LEARNINGS 2026-06-17 (same gap, C0.1). Promotion: QUEUED "Wire the CAMPPS bootstrap app into a guarded CD path" (P2).
- **Registry-driven minting is purely additive.** Adding 7 services produced a 40-resource, 0-modify, 0-delete CloudFormation diff — blast radius bounded to creation, never perturbation of the 4 live roles. Promotion: LEARNINGS 2026-06-20.
- **Provisioned-with-trust ≠ assume-proven.** Closing #134/#135 proves role provisioning + correct env-scoped trust, not that a live GitHub Actions job can assume the role (the 6 repos are bare scaffolds). Left explicit and unchecked in the #135 closing comment. Promotion: LEARNINGS 2026-06-20; live-assume deferred to C0.3-S5 E2E.
- **The "7 vs 10" card/build-program conflict was real and load-bearing.** Issue #134 is literally titled "register 7 service repos"; the canonical set (KTD1, from `phase-1a-build-program.md`) is 10 backends + web-app. Trusting the card text would have under-provisioned. Promotion: already in DECISIONS 2026-06-20.

## Diff vs last retro
- Prior retro: **none — this is the repo's first `/retro`.** No baseline to diff; this run establishes `docs/retros/`.
- Compounding signal to carry forward: the **bootstrap-app-outside-CD gap is now a 2x recurrence** (2026-06-17 C0.1, 2026-06-20 C0.3). The next retro should check whether the QUEUED automation shipped; if the gap appears a third time undeployed, escalate priority.

## Surfaced follow-ups
- → QUEUED (P2): wire `app_campps_bootstrap.py` into a guarded CD path or a drift-check (the recurring gap). **Filed this retro.**
- → covered elsewhere: #135 live-assume proof — folded into the open C0.3-S5 E2E slice (campps-platform#10), not separately queued (operator choice).
- → existing QUEUED (P2): staging/production CAMPPS role deploys remain pending a reviewer-gated `/deploy` (KTD5) — already tracked in "Bootstrap CAMPPS workload deploy roles before first service repo release".

## Proposed edits (Tier-2, dispositions)
- DECISIONS.md — mark C0.3 entry shipped + fill commit ref (PR #137 / `0a69c19` / nonprod-deploy / R5) — **applied.**
- QUEUED.md — add the CD-gap automation entry — **applied** (auto-class once approved as a route).
- LEARNINGS.md — additive-minting + provisioned-vs-assume entry — **applied** (Tier-1 auto append).

## Refs
- Saga: `issue-134` (read-only; 10 ticks, round 0, zero blockers).
- PRs: #137 (merged `0a69c19`). Issues: #134, #135 (both CLOSED 2026-06-20).
- Journal: LEARNINGS 2026-06-20, DECISIONS 2026-06-20 (updated), QUEUED P2 (new).
- Plan: `docs/plans/2026-06-20-c0-3-aws-infra-service-registry-oidc-plan.md`. Work session: `docs/work-sessions/2026-06-20-c0-3-service-registry-oidc.md`.
