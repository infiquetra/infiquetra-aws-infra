# GitHub OIDC Bootstrap Configuration
# Copy this file to .env and update with your values if needed

# AWS Account Configuration
# Primary Infiquetra account ID (should not change)
CDK_DEFAULT_ACCOUNT=645166163764
CDK_DEFAULT_REGION=us-east-1

# GitHub Repository Configuration
# These should match your repository settings exactly
GITHUB_OWNER=infiquetra
GITHUB_REPO=infiquetra-organizations

# Deployment Instructions:
# 1. Copy: cp .env.example .env
# 2. Deploy: uv run cdk deploy --profile infiquetra-root
# 3. The account ID above should match your AWS profile
#
# This creates:
# - GitHub OIDC Identity Provider
# - GitHubActionsDeployRole IAM role
# - Permissions for CDK deployments